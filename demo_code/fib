
	native func pow(a, b)

	func main(max) {
		if (isset max) == false {
			max = 1000
		}
		y = 1
		loop x: { if x >= max {break}
			print x, "\n"
			z = x + y
			x = y
			y = z
		}
		print "pow(2, 7) = ", ->pow(2, 7), "\n"
	}
