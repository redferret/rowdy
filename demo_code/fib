
	import "core.rowdy"
	
	func main(max) {
		if (isset max) == false {
			max = 1000
		}
		$println($fibby(max))
	}

	func fibby(max) {
		this.fibs = $List()
		this.z = 0
		this.y = 1
		loop x: { if x >= max {break}
			$add(fibs, x as long)
			z = x + y
			x = y
			y = z
		}
		return fibs
	}