
	import "core.rowdy"
	
	func main(max) {
		if (isset max) == false {
			max = 1000
		}
		$println($fibby(max))
	}

	func fibby(max) {
		fibs = $List()
		y = 1
		loop x: { if x >= max {break}
			$add(fibs, x as int)
			z = x + y
			x = y
			y = z
		}
		return fibs
	}